<%= form_with(model: student_class, data: { controller: 'select-form', select_form_url_value: '/student_classes/update_teachers' }, id: "#{dom_id(student_class)}_form") do |form| %>
  <% if student_class.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student_class.errors.count, "error") %> prohibited this student_class from being saved:</h2>

      <ul>
        <% student_class.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :teacher_id, style: "display: block" %>
    <%= form.collection_select :teacher_id, Teacher.all, :id, :name, { prompt: "Select teacher"}, id: "dynamic-select", data: { action: 'click->select-form#change', select_form_target: 'select' } %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
