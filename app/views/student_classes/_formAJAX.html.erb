<%= form_with(model: student_class) do |form| %>
  <% if student_class.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student_class.errors.count, "error") %> prohibited this student_class from being saved:</h2>

      <ul>
        <% student_class.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :teacher_id, style: "display: block" %>
    <%= form.collection_select :teacher_id, Teacher.all, :id, :name, { prompt: "Select teacher"}, id: "dynamic-select" %>

  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= javascript_include_tag 'ajaxform' %>
