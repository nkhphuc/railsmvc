<%= turbo_stream.replace "dynamic-select" do %>
  <%= collection_select :student_class, :teacher_id, Teacher.all, :id, :name, { prompt: "Select teacher", selected: params[:selected] }, id: "dynamic-select", data: { action: 'change->select-form#change', select_form_target: 'select' } %>
<% end %>
